---
name: git-commit
description: Generate and create git commits with good messages
allowed-tools: Bash(git status:*), Bash(git diff:*), Bash(git add:*), Bash(git commit:*)
---

## Task

1. Analyze current git changes
2. Group related changes
3. Generate a clear commit message using conventional commits
4. Show the message to the user
5. Ask for approval
6. If approved, run git add and git commit
