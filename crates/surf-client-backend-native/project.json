{
  "name": "surf-client-backend-native",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "root": "crates/surf-client-backend-native",
  "sourceRoot": "crates/surf-client-backend-native/src",
  "targets": {
    "build": {
      "executor": "@monodon/rust:build",
      "inputs": ["rust", "^rust"],
      "outputs": ["{options.target-dir}"],
      "options": {
        "lib": true
      }
    },
    "test": {
      "executor": "@monodon/rust:test",
      "inputs": ["rust", "^rust"],
      "outputs": ["{options.target-dir}"]
    }
  }
}
