{
  "name": "surf-provider-native",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "root": "crates/surf-provider-native",
  "sourceRoot": "crates/surf-provider-native/src",
  "targets": {
    "build": {
      "executor": "@monodon/rust:build",
      "inputs": ["rust", "^rust"],
      "outputs": ["{options.target-dir}"],
      "options": {
        "lib": true
      }
    },
    "test": {
      "executor": "@monodon/rust:test",
      "inputs": ["rust", "^rust"],
      "outputs": ["{options.target-dir}"]
    }
  }
}
