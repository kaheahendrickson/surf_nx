{
  "name": "test-rpc-validator",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "root": "crates/test-rpc-validator",
  "sourceRoot": "crates/test-rpc-validator/src",
  "targets": {
    "build": {
      "executor": "@monodon/rust:build",
      "inputs": ["rust", "^rust"],
      "dependsOn": ["^build"],
      "outputs": ["{options.target-dir}"]
    },
    "test": {
      "executor": "@monodon/rust:test",
      "inputs": ["rust", "^rust"],
      "outputs": ["{options.target-dir}"]
    },
    "run": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "cargo run --manifest-path crates/test-rpc-validator/Cargo.toml",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
